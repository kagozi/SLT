# Transfer Track B3
# Experiment 14: How2Sign — Low-data Transfer (1% training data, multistream)
# Pretrained from Exp 17 (PHOENIX multistream). Full fine-tune on 1% of How2Sign train set.
# Extreme low-resource scenario: tests whether cross-lingual pretrain enables few-shot SLR.
# Depends on: Exp 17 checkpoint at /data/experiments/models/exp17_phoenix_multistream/best_exp17_phoenix_multistream.pt
apiVersion: batch/v1
kind: Job
metadata:
  name: train-exp14-lowdata-1pct
  namespace: gai-lina-group
spec:
  backoffLimit: 1
  ttlSecondsAfterFinished: 86400
  activeDeadlineSeconds: 259200
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: trainer
        image: ghcr.io/kagozi/slt:latest
        command: ["python", "train.py"]
        args:
          - "--dataset"
          - "how2sign"
          - "--root_dir"
          - "/data/hf_cache/How2Sign_Holistic/how2sign_holistic_features"
          - "--output_dir"
          - "/data/experiments"
          - "--exp_name"
          - "exp14_lowdata_1pct"
          - "--epochs"
          - "100"
          - "--decode"
          - "beam"
          - "--beam_width"
          - "10"
          - "--batch_size"
          - "4"
          - "--grad_accum"
          - "4"
          - "--fp16"
          - "--dim"
          - "256"
          - "--max_frames"
          - "300"
          - "--num_workers"
          - "4"
          - "--multistream"
          - "--dropout"
          - "0.3"
          - "--pretrained_path"
          - "/data/experiments/models/exp17_phoenix_multistream/best_exp17_phoenix_multistream.pt"
          - "--freeze_strategy"
          - "none"
          - "--lr"
          - "5e-5"
          - "--subset_pct"
          - "1"
          - "--unglossed_ctc_target"
          - "translation_keywords"
        env:
        - name: WANDB_API_KEY
          valueFrom:
            secretKeyRef:
              name: wandb-credentials
              key: api-key
        - name: MPLCONFIGDIR
          value: /tmp/matplotlib
        - name: HF_HOME
          value: /data/.hf
        - name: TRANSFORMERS_CACHE
          value: /data/.hf/transformers
        - name: WANDB_CACHE_DIR
          value: /data/.wandb_cache
        resources:
          requests:
            cpu: "2"
            memory: "12Gi"
            nvidia.com/gpu: "1"
          limits:
            cpu: "2"
            memory: "14Gi"
            nvidia.com/gpu: "1"
        volumeMounts:
        - name: slt-data
          mountPath: /data
      volumes:
      - name: slt-data
        persistentVolumeClaim:
          claimName: slt-data
