# Priority 4
# Experiment 18: How2Sign — Multi-stream encoder (glossless)
# MultiStreamSignLanguageTransformer with glossless mBART translation.
# Per-stream velocity always included. Contrastive loss (w=0.1) shapes the
# encoder without needing pseudo-glosses.
# Updated for the repaired codepath: lower LR and lighter regularisation to
# reduce decoder collapse and unstable multistream optimisation.
# If Exp 18 >> Exp 16: multi-stream worth the compute for transfer exps.
apiVersion: batch/v1
kind: Job
metadata:
  name: train-exp18-how2sign-multistream
  namespace: gai-lina-group
spec:
  backoffLimit: 1
  ttlSecondsAfterFinished: 86400
  activeDeadlineSeconds: 864000
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: trainer
        image: ghcr.io/kagozi/slt:latest
        command: ["python", "train.py"]
        args:
          - "--dataset"
          - "how2sign"
          - "--root_dir"
          - "/data/hf_cache/How2Sign_Holistic/how2sign_holistic_features"
          - "--output_dir"
          - "/data/experiments"
          - "--exp_name"
          - "exp18_how2sign_multistream"
          - "--epochs"
          - "500"
          - "--decode"
          - "beam"
          - "--beam_width"
          - "10"
          - "--batch_size"
          - "4"
          - "--grad_accum"
          - "4"
          - "--fp16"
          - "--dim"
          - "256"
          - "--lr"
          - "1.5e-4"
          - "--dropout"
          - "0.3"
          - "--num_workers"
          - "4"
          - "--multistream"
          - "--use_bart"
          - "--ctc_weight"
          - "0.0"
          - "--freeze_bart_epochs"
          - "0"
          - "--ffn_expand"
          - "4"
          - "--label_smoothing"
          - "0.1"
          - "--contrastive_weight"
          - "0.05"
          - "--ctc_smoothing"
          - "0.0"
        env:
        - name: WANDB_API_KEY
          valueFrom:
            secretKeyRef:
              name: wandb-credentials
              key: api-key
        - name: MPLCONFIGDIR
          value: /tmp/matplotlib
        - name: HF_HOME
          value: /data/.hf
        - name: TRANSFORMERS_CACHE
          value: /data/.hf/transformers
        - name: WANDB_CACHE_DIR
          value: /data/.wandb_cache
        resources:
          requests:
            cpu: "4"
            memory: "40Gi"
            nvidia.com/gpu: "1"
          limits:
            cpu: "4"
            memory: "40Gi"
            nvidia.com/gpu: "1"
        volumeMounts:
        - name: slt-data
          mountPath: /data
      volumes:
      - name: slt-data
        persistentVolumeClaim:
          claimName: slt-data
