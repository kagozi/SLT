# DEPRECATED — How2Sign Kaggle Holistic download.
#
# The project now uses the official How2Sign RGB front clips instead.
# Data has been manually uploaded to the PVC at /data/how2sign_rgb/.
#
# Current How2Sign pipeline (run in order):
#   1. extract-how2sign-rgb-job.yaml        — unzip train/val/test clips
#   2. preextract-how2sign-keypoints-job.yaml — MediaPipe keypoint extraction
#   3. generate-pseudoglosses-job.yaml      — POS pseudoglosses (exp5 only)
#   4. train-exp4 / train-exp5
#
# This file is kept for reference only and should not be applied.
