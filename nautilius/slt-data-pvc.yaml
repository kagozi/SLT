apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: slt-data
  namespace: gai-lina-group
spec:
  storageClassName: rook-cephfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
